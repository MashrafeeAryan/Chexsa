[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "fridayv2"
version = "0.1.0"
description = "A general-purpose computer-use agent."
requires-python = ">=3.11"
